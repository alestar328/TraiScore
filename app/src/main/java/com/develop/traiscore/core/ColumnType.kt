package com.develop.traiscore.core

enum class ColumnType {
    SERIES,    // 1 cifra, no negativo
    WEIGHT,    // 3 cifras con decimales, no negativo
    REPS,      // 2 cifras, no negativo
    RIR        // 1 cifra, no negativo
}